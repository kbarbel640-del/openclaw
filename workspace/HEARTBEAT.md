# HEARTBEAT.md

目的：每次心跳推進專案、同步知識、監控系統健康。

入口指令：
- `python3 ~/clawd/scripts/health_check.py`
- `python3 ~/clawd/scripts/heartbeat_run.py --tier critical`
- `python3 ~/clawd/scripts/heartbeat_run.py --tier standard`
- `python3 ~/clawd/scripts/heartbeat_run.py --tier slow`
- `python3 ~/clawd/scripts/heartbeat_state.py`（節流 / 去重 / 通知冷卻）

核心流程：
1. 先跑健康檢查，確認關鍵服務狀態。
2. 依排程分級挑任務執行（critical / standard / slow）。
3. 推進 1-2 專案並更新 `PROJECT_REGISTRY.md`。
4. 必要時通知杜甫，最後記錄 dashboard。

分級排程：
- `critical`（每次或 <1m）：服務健康、Session 對齊快掃、記憶監控快掃、管線狀態、餘額紅線、**session 漏回檢測**。

## 🔍 Session 漏回檢測（每次心跳必做）

**為什麼**：防止「已讀不回」— 收到指令但沒行動。

**檢測方式**：
1. `sessions_list` 列出近 60 分鐘活躍 session
2. 檢查每個 session 最後一條 user 消息
3. 若是指令/問題但我回了 NO_REPLY → 需要跟進

**判斷標準**：
- 被 @ 或點名 → 必須回
- 明確指令（「幫我做X」「去查Y」）→ 必須行動
- 純閒聊/已完結 → NO_REPLY OK

**漏回時**：
1. **直接 @ 杜甫**（主 session）貼出疑似漏回清單（chat + sender + 摘要）
2. 用 `sessions_send` 補上行動或回覆（需要時）
- `standard`（每 15m）：專案輪值、需求追蹤、Skill Metrics。
- `slow`（每 4h / 每日）：全掃同步、素材品味、深度整理。

詳細規則與完整清單請看：`docs/heartbeat_playbook.md`
