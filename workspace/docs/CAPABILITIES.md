# 無極能力清單 (Capabilities Registry)

這份文件列出你所有可用的能力。**每次 session 啟動時讀取此檔。**

---

## ⚡ 執行命令

### exec-bridge（推薦）
繞過 Node.js EBADF 問題，用 Python subprocess 執行命令。

```bash
curl -s -X POST http://127.0.0.1:18793/exec \
  -H "Content-Type: application/json" \
  -d '{"command": "你的命令", "timeout": 60}'
```

| 項目 | 值 |
|------|-----|
| Port | 18793 |
| 健康檢查 | `curl http://127.0.0.1:18793/health` |
| 狀態 | ✅ 可用 |

### exec（原生，不穩定）
Clawdbot 內建的 exec 工具，有 EBADF 問題。**優先用 exec-bridge。**

---

## 📱 Telegram

### http_bridge（推薦）
HTTP API 存取杜甫的 Telegram 帳號。

```bash
# 健康檢查
curl http://127.0.0.1:18790/health

# 讀訊息
curl "http://127.0.0.1:18790/messages?chat=-5000326699&limit=10"

# 發訊息
curl -X POST http://127.0.0.1:18790/send \
  -d '{"chat": "-5000326699", "message": "內容"}'

# 列出聊天
curl "http://127.0.0.1:18790/chats?limit=30"
```

| 項目 | 值 |
|------|-----|
| Port | 18790 |
| 健康檢查 | `curl http://127.0.0.1:18790/health` |
| 狀態 | ✅ 可用 |

### message 工具（Bot）
用 @x01clawbot 發消息，適合回覆群組。

---

## 📋 Lark/飛書

### Browser MCP
直接操作 Lark 網頁，適合任務管理。

### Open API
程式化存取，已通過審批。詳見 `skills/lark/SKILL.md`。

---

## 🔧 自我診斷

**啟動時或遇到問題時，執行健康檢查：**

```bash
# 1. exec-bridge
curl -s http://127.0.0.1:18793/health

# 2. telegram http_bridge
curl -s http://127.0.0.1:18790/health

# 3. clawdbot gateway
curl -s http://127.0.0.1:18789/health
```

如果任何一個失敗，告訴杜甫：「我的 [能力名稱] 不通，需要檢查。」

---

## 🔄 同步協議

**如果你感覺「不知道自己能做什麼」，重新讀取這些檔案：**

1. `~/clawd/CAPABILITIES.md`（本檔）
2. `~/clawd/CLAUDE.md`
3. `~/clawd/TOOLS.md`
4. `~/clawd/memory/$(date +%Y-%m-%d).md`

---

## 📝 更新日誌

| 日期 | 更新 |
|------|------|
| 2026-01-28 | 新增 exec-bridge (port 18793) |
| 2026-01-28 | 停用 mcp-telegram，統一用 http_bridge |

---

*最後更新：2026-01-28*
