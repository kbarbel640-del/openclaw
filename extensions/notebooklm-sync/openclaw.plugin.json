{
  "id": "notebooklm-sync",
  "name": "NotebookLM Sync",
  "description": "Sync knowledge sources (web, PDF, Google Docs, YouTube) into OpenClaw memory.",
  "providers": ["notebooklm-sync"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "clientId": {
        "type": "string",
        "description": "Google OAuth 2.0 Client ID (Desktop app type). Create at https://console.cloud.google.com/apis/credentials"
      },
      "clientSecret": {
        "type": "string",
        "description": "Google OAuth 2.0 Client Secret."
      }
    }
  }
}
