{
  "name": "@openclaw/webhook",
  "version": "2026.2.4",
  "description": "OpenClaw webhook channel plugin for generic WebSocket connections",
  "type": "module",
  "devDependencies": {
    "openclaw": "workspace:*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
