{
  "id": "openclaw-guardian",
  "name": "OpenClaw Guardian",
  "description": "Security gate plugin — blacklist + LLM intent verification + tool-level safety. 99% of operations pass instantly with 0ms latency.",
  "version": "2.0.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": { "type": "boolean", "default": true }
    }
  }
}
