# Crontab Configuration for Local LLM Monitoring
# Add these entries to /etc/cron.d/local-llm-monitor
#
# Installation:
# sudo cp this-file /etc/cron.d/local-llm-monitor
# sudo chmod 644 /etc/cron.d/local-llm-monitor
#

SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin

# Health Check - Every 5 minutes
*/5 * * * * root /opt/openclaw/scripts/local-llm/llm-health-check.sh >> /var/log/llm-health.log 2>&1

# Metrics Collection - Every hour
0 * * * * root /opt/openclaw/scripts/local-llm/llm-metrics-collect.sh >> /var/log/llm-metrics.log 2>&1

# Daily restart (optional, only if needed for memory cleanup)
# 0 4 * * * root /usr/bin/systemctl restart local-llm

# Log rotation check - Daily at 2 AM
0 2 * * * root find /var/log/llm-*.log -mtime +7 -delete
