# Silicon Trader Workflow

> 目标：机器做减法，AI (Eden) 做决策。
> 根目录: `C:\Users\User\Desktop\openclaw\skills\silicon-trader`

---

## 🔄 核心作业流程 (SOP)

### Phase 1: 唤醒与感知 (Machine Layer)

**Trigger**: Cron (每15分钟) 或 用户指令。

1.  **运行脚本**:
    ```bash
    python C:\Users\User\Desktop\openclaw\skills\silicon-trader\data_feeder.py
    ```
2.  **获取输出 (JSON Feed)**:
    *   `market_state`: { balance, positions, spread_status }
    *   `radar_text`: "Asia Low: 4700 (SWEPT)..."
    *   `charts`: [H1_Path, M15_Path] (绝对路径)
3.  **异常检查**:
    *   *IF* `spread_status == "poor"` -> **⚠️ 警告**: 流动性差，降低信心分。
    *   *IF* `MT5 Connection Failed` -> **❌ 终止**: 报错并退出。

---

### Phase 2: 认知与推理 (Eden Layer)

**Input**: JSON Feed + 上次记忆 (`scan_records.json`)

#### Step 1: 记忆核实 (Recall & Re-validate)
*   **动作**: 
    *   调用 `python C:\Users\User\Desktop\openclaw\skills\silicon-trader\scan_memory.py` 获取最后一条记录。
*   **批判性核实**:
    *   不要只看价格是否到了目标。
    *   **必须检查失效位**: "价格有没有打穿我的止损/失效位？" -> 如果穿了，立刻标记 **❌ 逻辑作废**。
    *   **必须检查时间**: "这个计划是不是太久了？" -> 如果超过 4 小时，标记 **⚠️ 逻辑过期**。
    *   **结论**: 只有在结构完好且未过期的情况下，才标记为 **⏳ 等待触发**。否则直接**扔掉旧计划，重新分析**。

#### Step 2: 策略对照 (Context)
*   **动作**: 读取 `C:\Users\User\Desktop\openclaw\skills\silicon-trader\strategies\FINAL_SMC_STRATEGY.md`。
*   **⚠️ 数据铁律**: 
    *   **当前价格必须以 JSON 中的 `price` 字段为准！**
    *   **严禁** 试图从 H1 图表（可能复用旧图）或 M15 图表中肉眼估算价格。
*   **灵魂四问**:
    1.  **Liquidity**: 
    2.  **Bias**: 
    3.  **Value**: 
    4.  **Timing**: 
    
#### Step 3: 视觉复核 (Visuals)
*   **动作**: 读取 `C:\Users\User\Desktop\openclaw\skills\silicon-trader\strategies\VISUAL_ANALYSIS.md` 对照图表。
*   **故障检查**:
    *   *IF* 图表文件不存在 / 无法打开 / 全黑 / 数据错乱 -> **🚨 触发 [盲人不开车] 禁忌**。
    *   *Action*: 立即停止流程，向用户报错。
*   **正常检查**:
    *   暴力砸盘 (Displacement) - 机构进场
    *   阴跌/阴涨 (Grinding) - 散户陷阱
    *   扫荡针 (The Sweep Wick) - 猎杀信号
    *   实体突破 (Body Break) - 结构延续
---

### Phase 3: 决策与输出 (Decision Layer)

**决策优先级 (Decision Hierarchy)**:

1.  **优先思考 [主战场 (Pro-Trend)]**:
    *   顺着 H1 趋势方向，M15 有没有入场机会？
    *   *IF YES* -> **锁定主战场**，忽略游击战，直接进入执行环节。
    *   *IF NO* -> 进入下一步。

2.  **次要思考 [游击战 (Counter-Trend)]**:
    *   *条件*: 主战场无机会 (Wait) **且** 出现了明显的 HTF Sweep。
    *   有没有高盈亏比的逆势回调机会？
    *   *IF YES* -> **轻仓尝试**。
    *   *IF NO* -> **彻底观望**。

**Output**: 向用户发送 [分析报告]。

**⚠️ 强制要求**: 
1.  必须先发送 **H1 Context** 和 **M15 Trigger** 两张图表 (使用 JSON 中的路径)。
2.  报告必须包含 **双轨分析** 和 **信心评分计算**。

**信心评分标准 (源自 VISUAL_ANALYSIS.md)**:
*   **基础分**: 5 分
*   **加分**: 共振(+2), 极值区(+2), 强FVG(+2), Sweep(+2), Killzone(+1)
*   **减分**: 逆势(-2), 中轴(-1), 动能弱(-1), 结构乱(-1), 点差差(-2)
*   **分级**: 1-4(Skip) | 5-6(轻仓) | 7-8(标仓) | 9-10(重仓)

**报告模板 (严格执行)**:
    **动作**: 读取 `C:\Users\User\Desktop\openclaw\skills\silicon-trader\strategies\REPORT_TEMPLATE.md`

**自我审批 (Self-Review)**: 发送前检查逻辑一致性。

---

### Phase 4: 执行与记录 (Action Layer)

**Trigger**: 决策为“执行”且信心分 >= 5 (Auto-Trigger)。

1.  **计算参数**:
    *   `SL`: 扫荡点 + 10 pips。
    *   `TP`: 1:2。
    *   `Volume`: 账户余额 * 1% / 止损距离。
    *   *注意*: 根据信心分调整仓位 (0.5% / 1.0% / 1.5%)。
2.  **调用脚本**:
    ```bash
    python C:\Users\User\Desktop\openclaw\skills\silicon-trader\trade_executor.py --symbol XAUUSD --direction [BUY/SELL] --price [Limit] --sl [Price] --tp [Price] --volume [Lot] --reason "[Reason]"
    ```
    *(若为市价单，省略 --price)*
3.  **反馈**: "🚀 订单已提交，Ticket: 123456"。
4.  **无交易记录**: 如果观望，调用 `scan_memory.py` 写入本次判断。

---

## 🛠️ 故障排除

*   **图表模糊**: 检查 `chart_drawer.py` 的 DPI 设置。
*   **MT5 连不上**: 检查 Windows 端 MT5 是否开启 "Allow Algo Trading"。
*   **记忆错乱**: 手动删除 `C:\Users\User\.openclaw\workspace\smc\scan_memory\scan_records.json` 重置。

---

_Last Updated: 2026-02-06 (The Strict Standard)_
