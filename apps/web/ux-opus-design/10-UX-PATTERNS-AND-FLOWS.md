# UX Patterns and Flows

> Interaction patterns, user journeys, and flow diagrams

**Canonical URL/state + command palette IA:** `apps/web/ux-opus-design/16-STATE-NAV-AND-COMMAND-PALETTE.md`

---

## Core User Flows

### Flow 1: First-Time Agent Creation

```
┌─────────────────────────────────────────────────────────────────┐
│                    AGENT CREATION FLOW                           │
└─────────────────────────────────────────────────────────────────┘

     User clicks "Create Agent"
              │
              ▼
    ┌─────────────────────────────────────────┐
    │  Choose Setup Mode                      │
    │  ● Basic Setup (recommended)            │
    │  ○ Advanced Setup                       │
    └────────────────┬────────────────────────┘
                     │
         ┌───────────┴───────────┐
         │                       │
         ▼                       ▼
  (Basic Setup flow)      (Advanced Setup flow)

  Basic Setup:
    ┌─────────────────┐
    │  Step 1: Name   │     "What should we call this agent?"
    │  & Purpose      │     • Name input
    │                 │     • Role selection (tags)
    └────────┬────────┘     • Optional description
             │
             ▼
    ┌─────────────────┐
    │  Step 2: Tools  │     "What can this agent do?"
    │  Profile        │     • Minimal / Messaging / Coding / Full
    │                 │     • Quick explanation of each
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │  Step 3: Review │     "Here's your new agent"
    │  & Create       │     • Summary of choices
    │                 │     • "Use system defaults" for everything else
    └────────┬────────┘     • [Create Agent] button
             │
             ▼
    ┌─────────────────┐
    │  Agent Created  │     Redirect to agent detail page
    │  Success!       │     • Basics tab shown
    └─────────────────┘     • "Start a conversation" CTA
```

**Key UX Decisions:**
- Only 3 steps (under 2 minutes)
- No advanced options in wizard
- Everything else uses system defaults
- Can customize later from detail page

Advanced Setup is intentionally longer and preserves the existing “power” wizard behavior in `apps/web/src/components/domain/agents/CreateAgentWizard.tsx`.

Canonical dual-mode wizard plan:
- `apps/web/ux-opus-design/18-ONBOARDING-AND-WIZARDS.md`

---

### Flow 2: Customizing Agent Behavior

```
┌─────────────────────────────────────────────────────────────────┐
│                BEHAVIOR CUSTOMIZATION FLOW                       │
└─────────────────────────────────────────────────────────────────┘

     User navigates to Agent > Basics (Behavior section)
              │
              ▼
    ┌─────────────────────────────────────────┐
    │  Default State                          │
    │  [✓] Use system defaults                │
    │  Current: Creativity 0.7, Length Medium │
    └────────────────┬────────────────────────┘
                     │
        User clicks "Customize" or unchecks toggle
                     │
                     ▼
    ┌─────────────────────────────────────────┐
    │  Custom State                           │
    │  [ ] Use system defaults  [Reset]       │
    │                                         │
    │  Creativity: ───●─────── 0.3            │
    │  Length: [Short] [Med] [Long]           │
    │  Streaming: [ON]                        │
    └────────────────┬────────────────────────┘
                     │
              User edits draft values
                     │
                     ▼
    ┌─────────────────────────────────────────┐
    │  Unsaved changes                        │
    │  [Discard]                 [Save changes]│
    └─────────────────────────────────────────┘

     (on save success)
                     ▼
    ┌─────────────────────────────────────────┐
    │  Saved                                  │
    │  ✓ Changes saved                        │
    └─────────────────────────────────────────┘

     (on save error)
                     ▼
    ┌─────────────────────────────────────────┐
    │  Save failed                            │
    │  Failed to save changes                 │
    │  [Try again]  [Copy changes]            │
    └─────────────────────────────────────────┘
```

**Key UX Decisions:**
- Default state is "use system defaults"
- Single click to enable customization
- Draft values with an explicit commit point (Save/Discard)
- Easy reset back to defaults

---

### Flow 3: Connecting a Provider

```
┌─────────────────────────────────────────────────────────────────┐
│                  PROVIDER CONNECTION FLOW                        │
└─────────────────────────────────────────────────────────────────┘

     User clicks "Connect" on provider card
              │
              ▼
    ┌─────────────────────────────────────────┐
    │  Choose Auth Method                     │
    │  ○ API key                              │
    │  ○ Sign in with OAuth                   │
    └────────────────┬────────────────────────┘
                     │
         ┌───────────┴───────────┐
         │                       │
         ▼                       ▼
┌─────────────────┐    ┌─────────────────────────┐
│  API Key Flow   │    │  OAuth Flow             │
│                 │    │                         │
│  [Enter key]    │    │  [Sign in with OAuth]   │
│  [Test]         │    │                         │
│                 │    │  Opens browser/popup    │
└────────┬────────┘    └───────────┬─────────────┘
         │                         │
         ▼                         ▼
    ┌─────────────────────────────────────────┐
    │  Test Connection                        │
    │  ● Testing...                           │
    │                                         │
    │  ✓ Connected successfully!              │
    │  Available models: Claude Sonnet, ...   │
    └────────────────┬────────────────────────┘
                     │
                     ▼
    ┌─────────────────────────────────────────┐
    │  Select Default Model (optional)        │
    │  [Claude Sonnet ▼]                      │
    │                                         │
    │  [Done]                                 │
    └─────────────────────────────────────────┘

     (failure examples)
     ┌─────────────────────────────────────────┐
     │  Test failed                             │
     │  Can't reach provider / invalid key      │
     │  [Try again]  [View details]             │
     └─────────────────────────────────────────┘

     ┌─────────────────────────────────────────┐
     │  Models list fetch failed                │
     │  We couldn't load models right now.      │
     │  [Retry]  (Expert: enter model id)       │
     └─────────────────────────────────────────┘
```

**Key UX Decisions:**
- Immediate feedback on connection test
- Show available models after connection
- Optional default model selection
- Credentials are secrets (mask by default; reveal/copy are explicit and auditable)
- Explicit error states: save failed, test failed, models list fetch failed
- Fallback to pairing for headless/cloud gateway setups (no browser available)

---

### Flow 4: Creating a Toolset

```
┌─────────────────────────────────────────────────────────────────┐
│                   TOOLSET CREATION FLOW                          │
└─────────────────────────────────────────────────────────────────┘

     User clicks "Create Toolset" in Settings > Toolsets
              │
              ▼
    ┌─────────────────────────────────────────┐
    │  Name Your Toolset                      │
    │  [Research Mode                    ]    │
    │                                         │
    │  Description (optional)                 │
    │  [Tools for web research and...]        │
    └────────────────┬────────────────────────┘
                     │
                     ▼
    ┌─────────────────────────────────────────┐
    │  Configure Tools                        │
    │                                         │
    │  Start from:                            │
    │  ○ Blank (no tools)                     │
    │  ● Standard preset                      │
    │  ○ Copy from existing toolset           │
    │                                         │
    │  ▼ Files & Documents          3 of 6    │
    │    ☑ Read  ☑ Search  ☐ Write  ☐ Delete │
    │  ▶ Code & Development         0 of 5    │
    │  ▼ Data & Research            4 of 4    │
    │    ☑ Web search  ☑ Fetch  ☑ ...        │
    └────────────────┬────────────────────────┘
                     │
                     ▼
    ┌─────────────────────────────────────────┐
    │  Review & Save                          │
    │                                         │
    │  "Research Mode"                        │
    │  7 tools enabled                        │
    │                                         │
    │  [Cancel]              [Create Toolset] │
    └─────────────────────────────────────────┘
```

**Key UX Decisions:**
- Start from presets, not blank
- Collapsible categories
- Summary before saving
- Can be edited later

---

## Interaction Patterns

### Pattern 1: Inline Editing

Most configuration uses inline editing rather than modals.

```
┌─────────────────────────────────────────────────────────────────┐
│                      INLINE EDIT PATTERN                         │
└─────────────────────────────────────────────────────────────────┘

READ STATE:
┌─────────────────────────────────────────┐
│ Agent name                              │
│ Research Bot                     [Edit] │
└─────────────────────────────────────────┘

EDIT STATE:
┌─────────────────────────────────────────┐
│ Agent name                              │
│ [Research Bot            ] [Save] [✕]   │
└─────────────────────────────────────────┘

SAVING STATE:
┌─────────────────────────────────────────┐
│ Agent name                              │
│ [Research Bot            ] ● Saving...  │
└─────────────────────────────────────────┘

SAVED STATE:
┌─────────────────────────────────────────┐
│ Agent name                              │
│ Research Bot ✓                   [Edit] │
└─────────────────────────────────────────┘
```

---

### Pattern 2: Accordion Progressive Disclosure

```
┌─────────────────────────────────────────────────────────────────┐
│                    ACCORDION PATTERN                             │
└─────────────────────────────────────────────────────────────────┘

COLLAPSED (default for Advanced):
┌─────────────────────────────────────────┐
│ ▶ Advanced                              │
└─────────────────────────────────────────┘

EXPANDED:
┌─────────────────────────────────────────┐
│ ▼ Advanced                              │
├─────────────────────────────────────────┤
│                                         │
│   Model override: [Anthropic] [Opus]    │
│   Runtime: ○ Default ○ Pi ○ SDK         │
│                                         │
└─────────────────────────────────────────┘
```

**Rules:**
- Primary options always visible
- Advanced collapsed by default
- Expert mode: Advanced expanded by default
- Remember expanded state in session

---

### Pattern 3: Confirmation for Dangerous Actions

```
┌─────────────────────────────────────────────────────────────────┐
│                  DANGEROUS ACTION PATTERN                        │
└─────────────────────────────────────────────────────────────────┘

User toggles dangerous setting (e.g., disable sandbox):

┌─────────────────────────────────────────────────────────────────┐
│ ⚠️ Disable Sandbox?                                              │
│                                                                 │
│ Disabling the sandbox allows this agent direct access to your   │
│ workspace and system. Only do this if you trust this agent      │
│ completely.                                                     │
│                                                                 │
│ [Cancel]                              [I understand, disable]   │
└─────────────────────────────────────────────────────────────────┘
```

**Dangerous actions requiring confirmation:**
- Disable sandbox
- Enable elevated mode
- Enable system commands
- Delete agent
- Delete toolset in use

---

### Pattern 4: Empty States

```
┌─────────────────────────────────────────────────────────────────┐
│                     EMPTY STATE PATTERN                          │
└─────────────────────────────────────────────────────────────────┘

NO AGENTS:
┌─────────────────────────────────────────────────────────────────┐
│                          🤖                                      │
│                                                                 │
│                  Create your first agent                        │
│                                                                 │
│     Agents are AI assistants that can help with tasks,          │
│     answer questions, and automate workflows.                   │
│                                                                 │
│                    [+ Create Agent]                             │
│                                                                 │
│           [Learn about agents →]                                │
└─────────────────────────────────────────────────────────────────┘

NO PROVIDERS:
┌─────────────────────────────────────────────────────────────────┐
│                          🔌                                      │
│                                                                 │
│                 Connect a model provider                        │
│                                                                 │
│     You need at least one AI provider (like Anthropic or        │
│     OpenAI) before agents can respond.                          │
│                                                                 │
│                  [Connect Anthropic]                            │
│                  [Connect OpenAI]                               │
│                                                                 │
│           [Learn about providers →]                             │
└─────────────────────────────────────────────────────────────────┘
```

---

### Pattern 5: Validation Feedback

```
┌─────────────────────────────────────────────────────────────────┐
│                   VALIDATION PATTERN                             │
└─────────────────────────────────────────────────────────────────┘

INLINE ERROR:
┌─────────────────────────────────────────┐
│ API key                                 │
│ [sk-invalid...                     ] ⚠️ │
│ ⚠️ This key is invalid or expired.      │
│    [Test again]                         │
└─────────────────────────────────────────┘

FIELD REQUIREMENT:
┌─────────────────────────────────────────┐
│ Agent name *                            │
│ [                                  ]    │
│ ⚠️ Name is required                     │
└─────────────────────────────────────────┘

SUCCESS VALIDATION:
┌─────────────────────────────────────────┐
│ API key                                 │
│ [sk-ant-...                        ] ✓  │
│ ✓ Connected successfully                │
└─────────────────────────────────────────┘
```

---

## Power User Workflows (Required)

These workflows must be explicitly supported in the UX (not just possible via manual clicking):

1) **Clone agent definition**
   - Copy an agent’s config (including overrides) into a new agent.
   - Must allow choosing which parts to clone (identity only vs behavior/tools/memory/etc.).

2) **Config diff (agent vs system defaults)**
   - A readable diff that shows:
     - inherited values
     - overridden values
     - reset actions per field group

3) **Overrides management**
   - A central “Overrides” panel/list that answers:
     - what is overridden
     - where it lives (config path)
     - how to reset
   - “Reset all overrides” must exist, with confirmation.

4) **Export/import**
   - Export agent config (JSON) and import it back (with validation).

These are anchored in the **Configuration Summary** surface described in `apps/web/ux-opus-design/08-AGENT-CONFIGURATION-DESIGN.md`.

## Navigation Patterns

### Breadcrumb Navigation

```
Settings > Model & Provider
Agents > Research Bot > Behavior
```

### Search / Jump (Configuration Command Palette)

Power users must be able to jump directly to:
- any agent
- any agent tab
- any settings section
- provider connect/test flows
- toolset edit flows

Entry: `Cmd+K` / `Ctrl+K` and a visible “Search / Jump” button in config surfaces.

See: `apps/web/ux-opus-design/16-STATE-NAV-AND-COMMAND-PALETTE.md`.

### Tab Persistence

When navigating away and back to an agent detail page, return to the last viewed tab.

```typescript
// URL: /agents/abc123?tab=behavior
// Store in session: lastAgentTab['abc123'] = 'behavior'
```

### Deep Links

All major views should be directly linkable:

```
/settings?section=model-provider
/agents/abc123?tab=basics
/agents/abc123?tab=more
/agents/abc123?tab=tools
/agents/abc123?tab=advanced
```

---

## Loading States

### Skeleton Loading

```
┌─────────────────────────────────────────────────────────────────┐
│ ████████████                                                    │
│ ██████████████████████████████████████                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ████████████████████                                           │
│  ████████████████████████████████████████████                   │
│  ──────────────────────────────────────────                     │
│                                                                 │
│  ████████████████████                                           │
│  ████████████████████████████████████                           │
│  ──────────────────────────────────────────                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Loading Indicators

- Page load: Full skeleton
- Section load: Inline skeleton
- Action in progress: Button spinner
- Background save: Subtle status indicator

---

## Error Handling

### Error Recovery

```
┌─────────────────────────────────────────────────────────────────┐
│ ⚠️ Failed to save changes                                        │
│                                                                 │
│ We couldn't save your changes. This might be a temporary        │
│ network issue.                                                  │
│                                                                 │
│ [Try again]  [Discard changes]  [Copy to clipboard]             │
└─────────────────────────────────────────────────────────────────┘
```

### Explicit Error States (Required)

These error states must be explicitly designed and implemented (not left implicit):

1) **Save failed**
   - Must show: what failed, whether changes are still in the UI, recovery actions (retry/undo/copy).

2) **Test failed** (provider connection tests)
   - Must show: failure reason (safe message), last tested, suggested next steps, and “re-test” CTA.

3) **Models list fetch failed**
   - Must show: “can’t load models” state separate from “no models exist”.
   - Must provide retry and allow saving unrelated fields.

### Offline Handling

```
┌─────────────────────────────────────────────────────────────────┐
│ 📡 You're offline                                                │
│                                                                 │
│ Changes will be saved when you reconnect.                       │
│                                                                 │
│ [Dismiss]                                                       │
└─────────────────────────────────────────────────────────────────┘
```
