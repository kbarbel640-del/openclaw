import type { IncomingMessage, ServerResponse } from "node:http";

export async function handleAccountTagsHttpRequest(
  _req: IncomingMessage,
  _res: ServerResponse,
): Promise<boolean> {
  // TODO: implement
  return false;
}
